#!/usr/bin/env bash

#iwctl station wlan0 scan > /dev/null
#networks=$(iwctl station wlan0 get-networks | rg -oP '^[ >]+\K.*?(?=  {2,})' | tail -n +2)
#selected=$(printf "\n$networks" | dmenu -i -p "Escolher rede:")
#[[ -z "$selected" ]] && exit 1
#password=$(echo "" | dmenu -p "Senha ($selected):")

echo "󰖩 100% CLARO_5G98745E"
#echo "󰖪 off"
